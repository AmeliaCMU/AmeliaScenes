defaults:
  - _self_
  - mode: full  # Pick type of processing to run ["full", "metas", "scenes"]
  - presets: amelia_10  # Pick a preset to run ["amelia_10", "amelia_48", "sample"]
  - override hydra/job_logging: disabled
  - override hydra/hydra_logging: disabled # Disable output logging