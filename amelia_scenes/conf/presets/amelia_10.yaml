# @package _global_
# SETUP PATH NAMES
airport: [
  kbos,
  kewr,
  kdca,
  kjfk,
  klax,
  kmdw,
  kmsy,
  ksea,
  ksfo,
  panc
] # ICAO code or use keyword all for all airports
base_dir: datasets/amelia
traj_version: a10v08
graph_version: a10v01os
out_version: null # Set to null to omit versioning for output

# PROCESSING SETTINGS
parallel: False
overwrite: True
benchmark: False
xplane: False
perc_process: 1.0
seed: 42
jobs: -1

# SCENE PROCESSING SETTINGS
pred_lens: [20, 50]
hist_len: 10
skip: 1
min_agents: 2
max_agents: 40
min_valid_points: 2
